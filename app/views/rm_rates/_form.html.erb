<%= form_for(@rm_rate) do |f| %>
  <% if @rm_rate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rm_rate.errors.count, "error") %> prohibited this rm_rate from being saved:</h2>

      <ul>
        <% @rm_rate.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%#= f.label :code %><br />
    <%#= f.text_field :code %>
  </div>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :rate %><br />
    <%= f.text_field :rate, :size=>"8" %> %
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
