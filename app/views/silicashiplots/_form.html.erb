<%= form_for(@silicashiplot) do |f| %>
  <% if @silicashiplot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@silicashiplot.errors.count, "error") %> prohibited this silicashiplot from being saved:</h2>

      <ul>
      <% @silicashiplot.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :lot %><br />
    <%= f.text_field :lot %>
  </div>
  <div class="field">
    <%= f.label :weight %><br />
    <%= f.text_field :weight %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
