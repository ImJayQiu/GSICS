<% if can? :manage, :unit %>

  <%= form_for(@purchase_unit) do |f| %>
    <% if @purchase_unit.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@purchase_unit.errors.count, "error") %> prohibited this purchase_unit from being saved:</h2>

        <ul>
          <% @purchase_unit.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= f.label :code %><br>
      <%= f.text_field :code %>
    </div>
    <div class="field">
      <%= f.label :unit %><br>
      <%= f.text_field :unit %>
    </div>
    <div class="field">
      <%= f.label :remark %><br>
      <%= f.text_field :remark %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>

<% else %>

  <%= render "site/denied" %>

<% end %>