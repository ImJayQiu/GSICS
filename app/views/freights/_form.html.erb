<% if can? :manage, :po %>


  <%= form_for(@freight) do |f| %>
    <% if @freight.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@freight.errors.count, "error") %> prohibited this freight from being saved:</h2>

        <ul>
          <% @freight.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= f.label :name %><br />
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :contact %><br />
      <%= f.text_field :contact %>
    </div>
    <div class="field">
      <%= f.label :phone %><br />
      <%= f.text_field :phone %>
    </div>
    <div class="field">
      <%= f.label :email %><br />
      <%= f.text_field :email %>
    </div>
    <div class="field">
      <%= f.label :address %><br />
      <%= f.text_field :address %>
    </div>
    <div class="field">
      <%= f.label :remark %><br />
      <%= f.text_field :remark %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>


<% else %>

  <%= render "site/denied"  %>

<% end %>