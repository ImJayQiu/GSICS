<% if can? :manage, :po %>


  <h2>Editing Freight Company</h2>

  <%= render 'form' %>

  <%= link_to 'Show', @freight %> |
  <%= link_to 'Back', freights_path %>

<% else %>

  <%= render "site/denied"  %>

<% end %>