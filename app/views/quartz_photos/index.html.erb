<h1>Listing quartz_photos</h1>

<table>
  <tr>
    <th>Quartz</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @quartz_photos.each do |quartz_photo| %>
  <tr>
    <td><%= quartz_photo.quartz_id %></td>
    <td><%= link_to 'Show', quartz_photo %></td>
    <td><%= link_to 'Edit', edit_quartz_photo_path(quartz_photo) %></td>
    <td><%= link_to 'Destroy', quartz_photo, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Quartz photo', new_quartz_photo_path %>
