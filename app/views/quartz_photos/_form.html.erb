<%= form_for(@quartz_photo) do |f| %>
  <% if @quartz_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quartz_photo.errors.count, "error") %> prohibited this quartz_photo from being saved:</h2>

      <ul>
      <% @quartz_photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :quartz_id %><br />
    <%= f.text_field :quartz_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
